import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  paper: {
    display: 'flex',
    alignItems: 'center',
    minHeight: '97vh',
  },
  contentContainer: {
    flexGrow: 1,
  },
}));
